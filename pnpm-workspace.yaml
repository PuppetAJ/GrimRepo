packages:
  - client
  - server
  - shared

# pnpm runs a dependency's install script only if it is listed here.
allowBuilds:
  # The Railway CLI bundles the config file with esbuild, which fetches its binary on install.
  esbuild: true
