{
  "name": "server",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Grim Repo API",
  "license": "MIT",
  "main": "src/server.ts",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=../.env src/server.ts",
    "start": "node --env-file-if-exists=../.env src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test --import ./src/test/env.ts 'src/**/*.test.ts'"
  },
  "dependencies": {
    "compression": "^1.8.2",
    "express": "^5.2.1",
    "helmet": "^8.3.0",
    "pg": "^8.23.0",
    "shared": "workspace:*",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/compression": "^1.8.1",
    "@types/express": "^5.0.6",
    "@types/node": "^26.5.1",
    "@types/pg": "^8.23.1",
    "typescript": "^7.0.2"
  }
}
